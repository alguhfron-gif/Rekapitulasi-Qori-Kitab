rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Presensi MTK Musyawarah collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

